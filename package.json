{
  "name": "action-download-iab-lists",
  "version": "1.0.0",
  "description": "fetch files from IAB periodically for local hosting or validation purposes",
  "main": "lib/index.js",
  "scripts": {
    "start": "npm run build && node lib/index.js",
    "build": "tsc --noEmit && esbuild --bundle src/index.ts --platform=node --outfile=lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@actions/core": "1.10.1",
    "@types/node": "20.8.10",
    "esbuild": "0.19.5",
    "typescript": "5.2.2"
  }
}
